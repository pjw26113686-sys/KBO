"""데이터 수집 모듈 (설계서 모듈 1 / §2).

⚠️ 실 네트워크 호출은 구현하지 않는다(stub). Statiz 무단 사용은 법적 조치 명시,
KBO 동적 페이지는 봇 차단 가능성 — 이 환경에서 동작 검증 불가. 여기서는 데이터
*계약(반환 스키마)* 만 정의하고, 실제 구현 시 rate limit(요청 간 2~5초), User-Agent
설정, robots.txt 준수, 개인 용도 한정을 반드시 지킬 것(설계서 §2).
"""
